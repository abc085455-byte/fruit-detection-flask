# 🍎 Fruit Detection System (Computer Vision Project)

This project is a **Fruit Detection System** built using **Deep Learning and Computer Vision**.  
It detects multiple fruits from images using a **pre-trained object detection model** and shows bounding boxes with labels.

---

## 🚀 Project Features

- Detects **multiple fruits** in a single image
- Supports **Apple, Banana, Grapes, Orange** (can be extended)
- Uses **state-of-the-art object detection model**
- Simple and clean **Flask-based web interface**
- Real-time prediction on uploaded images

---

## 🧠 Technologies Used

- **Python**
- **Flask**
- **PyTorch**
- **RT-DETR / YOLO-based Object Detection**
- **OpenCV**
- **HTML, CSS (Frontend)**

---

## 📂 Project Structure

